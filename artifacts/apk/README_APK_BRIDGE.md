# APK Bridge

The APK is optional and is kept as a runtime harness. Researchers should start from the GitHub workbench and evidence docs.

Use the APK only when Android report generation is needed. Import generated report text into the workbench for reading, history, copy/download/print, and calibration-ledger indexing.
